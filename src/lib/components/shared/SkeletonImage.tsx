import Image, { ImageProps } from 'next/image';
import React, { forwardRef, Ref } from 'react';

const SkeletonImage = () => {
  return <div className='skeleton w-100 h-100'></div>
};

export default SkeletonImage;
